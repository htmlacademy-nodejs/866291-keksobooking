'use strict';

const AVATAR_URL = `https://robohash.org/`;

const LOCATION = {
  MAX_X: 900,
  MIN_X: 300,
  MAX_Y: 500,
  MIN_Y: 150
};

const DATE_INTERVAL = -7;

module.exports = {
  AVATAR_URL,
  LOCATION,
  DATE_INTERVAL
};
